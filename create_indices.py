from generate import database

id = database.index()

id["player_id", "player_id"] = ["player_id"]
id["prediction_id", "prediction_id"] = ["data.tracked.data.response.decoded.prediction_id"]
id["explanation_id", "explanation_id"] = ["data.tracked.data.response.decoded.explanation_id"]
